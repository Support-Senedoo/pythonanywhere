# Copier les variables utiles dans l’onglet Web PythonAnywhere (ou .env local pour tests).

TOOLBOX_SECRET_KEY=generer_une_longue_chaine_aleatoire

# Version affichée (optionnel — sinon défaut dans web_app/app_version.py).
# IMPORTANT : ne pas mettre 1, 1.0, 1.0.0 ni une seule valeur sans sens — l’appli les ignore et reprend le défaut du code (voir web_app/app_version.py).
# TOOLBOX_APP_VERSION=1.4.0
# Forcer le rechargement des templates .html à chaque requête (hors PA, si besoin) :
# TOOLBOX_TEMPLATE_AUTO_RELOAD=1
# Désactiver le cache LRU Jinja (déjà activé via pythonanywhere_wsgi.py / pa_wsgi.py sur PA) :
# TOOLBOX_JINJA_NO_CACHE=1
#
# Reload auto après deploy_pa.sh (token jamais ici ni dans Git) :
# Sur PA : echo 'VOTRE_TOKEN' > ~/.pythonanywhere_api_token && chmod 600 ~/.pythonanywhere_api_token
# EU : echo 'https://eu.pythonanywhere.com' > ~/.pythonanywhere_api_host && chmod 600 ~/.pythonanywhere_api_host
# Ou : export PYTHONANYWHERE_API_TOKEN=…  et  export PYTHONANYWHERE_API_HOST=…  (Bash interactif)
# Optionnel : PYTHONANYWHERE_WEBAPP_HOST  PYTHONANYWHERE_API_USER
# TOOLBOX_APP_DATE=2026-04-03
# TOOLBOX_APP_LABEL=Toolbox Senedoo
# TOOLBOX_APP_AUTHOR=Senedoo
# Chemins optionnels (défaut : toolbox_users.json et toolbox_clients.json à la racine du projet)
# TOOLBOX_USERS_PATH=/home/VOTRE_USER/odoo-pythonanywhere/toolbox_users.json
# TOOLBOX_CLIENTS_PATH=/home/VOTRE_USER/odoo-pythonanywhere/toolbox_clients.json
#
# Script XML-RPC console (scripts/xmlrpc_toolbox_client.py) : même registre que la toolbox.
# Mettre le nom de base choisi dans l’écran staff (identique à la liste déroulante) :
# TOOLBOX_XMLRPC_CLIENT_ID=nom_base_odoo
# Ou fichier dont la 1re ligne = ce nom (cron / Bash) :
# TOOLBOX_XMLRPC_CLIENT_ID_FILE=/home/VOTRE_USER/odoo-pythonanywhere/.toolbox_xmlrpc_client_id

# Génération des hash mots de passe :
#   python toolbox_generate_password_hash.py
# Coller le résultat dans toolbox_users.json (champ password_hash).

# Comptes démo code : test / passer (client) + support@senedoo.com / 2026@Senedoo (staff) — désactiver en prod :
# TOOLBOX_DISABLE_DEV_LOGIN=1
# Client démo associé au registre (id dans toolbox_clients.json) :
# TOOLBOX_TEST_CLIENT_ID=la_ripaille

# --- Réinitialisation mot de passe (/forgot-password) : SMTP + URL publique ---
# URL du site SANS slash final (liens dans les e-mails). Obligatoire si le domaine déduit de la requête est faux (proxy).
# TOOLBOX_PUBLIC_BASE_URL=https://senedoo.pythonanywhere.com
# Fichier jetons (défaut : même répertoire que toolbox_users.json). Optionnel :
# TOOLBOX_PASSWORD_RESET_TOKENS_PATH=/home/VOTRE_USER/pythonanywhere/odoo-pythonanywhere/toolbox_password_reset_tokens.json
#
# TOOLBOX_SMTP_HOST=smtp.gmail.com
# TOOLBOX_SMTP_PORT=587
# TOOLBOX_SMTP_USER=votre_compte@gmail.com
# TOOLBOX_SMTP_PASSWORD=mot_de_passe_application_gmail
# TOOLBOX_MAIL_FROM=noreply@votredomaine.com
#
# Comptes gratuits PythonAnywhere : pas d’e-mail sortant « générique » ; utiliser un relais (Gmail app password,
# SendGrid, Mailgun, SMTP du registrar, etc.). Voir aussi l’aide PA sur l’envoi d’e-mails.

# Bases Odoo « gérées » (suggestions dans le champ Base lors de l’édition client, séparateur virgule ou ;)
# À remplir avec les noms techniques des bases accessibles au compte API (ex. support@senedoo.com).
# Ne pas mettre de mot de passe ici — uniquement dans toolbox_clients.json (champ chiffré côté serveur si possible).
# TOOLBOX_ODOO_MANAGED_DATABASES=base_client_a,base_client_b

# Placeholder du champ « Login Odoo » dans le formulaire client (optionnel, défaut support@senedoo.com)
# TOOLBOX_ODOO_DEFAULT_API_USER=support@senedoo.com

# Utilitaire « Sonde bases » — mode sans URL (portail odoo.com / Mes bases), rarement à changer :
# TOOLBOX_ODOO_PORTAL_ORIGIN=https://www.odoo.com
# TOOLBOX_ODOO_PORTAL_LANG=/fr_FR

# Langue Odoo pour l’utilitaire « Rapports comptables » + script personalize_syscohada_detail.py (défaut fr_FR)
# TOOLBOX_ODOO_LANG=fr_FR
